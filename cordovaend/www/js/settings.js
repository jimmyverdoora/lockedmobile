var apiurl = "http://localhost:8080";

var currentVersion = "1.0.0"; // aggiornare sempre anche server e config.xml

var errorMsg = "<p>Well... :(<br/>An error occurred during the comunication with our server. If you see this, the error has already been reported and we are probably working to fix it ;)</p>";